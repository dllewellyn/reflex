{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Interaction Event",
  "description": "A raw interaction event between a user and the model",
  "type": "object",
  "properties": {
    "interaction_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the interaction"
    },
    "conversation_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the conversation session"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the interaction occurred (UTC)"
    },
    "role": {
      "type": "string",
      "enum": ["user", "model"],
      "description": "The role of the entity generating the content"
    },
    "content": {
      "type": "string",
      "description": "The text content of the interaction"
    }
  },
  "required": [
    "interaction_id",
    "conversation_id",
    "timestamp",
    "role",
    "content"
  ]
}
